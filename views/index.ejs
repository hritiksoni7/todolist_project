<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>todo app</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Rubik&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
    <link rel="stylesheet" href="styles/main.css">

</head>
<body>
  <img src="/images/download.jpg" alt="">
    <h1> todo list</h1>
  <div class="container1">
    
    <% var d=new Date(); var date=d.getDate(); var mon=d.getMonth()+1;var year=d.getFullYear(); %>
    <h2 class="date">DATE :- <%= date%>/<%= mon%>/<%= year%> </h2>
    <!-- adding new task -->
    <form action ="/addtask" method="POST">

      <!-- <input type="text" name="newtask" placeholder="add new task">
      <button class="btn-primary"> Add Task </button> -->
      <!-- bootstap -->
      <div class="input-group mb-3">
        <input type="text" class="form-control" name="newtask" placeholder="add new task" aria-label="Recipient's username" aria-describedby="button-addon2">
        <button class="btn btn-outline-dark">Add Task</button>
      </div>
      </form>
      <!-- showing task checkbox -->
      <h2 class="tasks" >Tasks : </h2>
        
      <form action="/complete" method="post">
     
      
      <% for( var i = 0; i < task.length; i++){ %>
        <li><input type="checkbox" name="check" value="<%= task[i] %>" /> <%= task[i] %> </li>
      <% } %>
      

      <button class="btn btn-outline-dark btnCompleted" formaction="/complete">check completed</button>  
    </form>
    

    <h2 class="complete">Completed tasks:</h2>
    <% for( var i = 0; i < complete.length; i++){ %>
      <li> <%= complete[i] %> </li>
    <% } %>
    <form action="/clear" method="post">
    <button class="btn btn-outline-dark btnCompleted" formaction="/clear">clear list</button>  
  </form>    
  </div>


</body>
</html>